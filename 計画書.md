## 目的
`livox/imu`を購読して角速度を積分し、roll/pitch/yawを求めつつ位置も積分して矢印マーカーで可視化するノードを作る。ノイズ対策としてバイアス推定、角速度クリッピング、ローパス、dtチェックを実装し、ON/OFFや閾値はパラメータ化する。

## 実装方針
- C++17 / rclcpp / sensor_msgs::Imu / visualization_msgs::Marker を使用。
- パラメータ（例）
  - `imu_topic` (string, default `livox/imu`)
  - `bias_sample_count` (int, default 200) ※バイアス推定は常に有効
  - `clip_enable` (bool, default true), `clip_max_rad_per_s` (double, default 5.0)
  - `lpf_enable` (bool, default true), `lpf_alpha` (double, default 0.2)
  - `dt_check_enable` (bool, default true), `dt_min` (double, default 0.0005), `dt_max` (double, default 0.05)
  - `marker_frame` (string, default `livox_frame`), `marker_topic` (string, default `imu_arrow`)
  - `use_gravity` (bool, default false) ※今回は単純な速度/位置積分のみ、必要なら重力補正を拡張
- 処理フロー
  1. IMU受信 → dt算出（dtチェック有効なら範囲外は破棄）
  2. バイアス推定（初期Nサンプル平均）→角速度から引く（有効時）
  3. 角速度クリッピング（有効時）
  4. ローパス（IIR, 有効時）
  5. roll/pitch/yawを微小角近似のクォータニオン積分で更新
  6. 速度・位置を単純積分（加速度未使用 or 後続対応）
  7. Marker(ARROW)として位置と姿勢をpublish
- 初期位置・姿勢はゼロ、リセットが必要ならサービス/パラメータ更新で拡張余地を残す。

## テスト方針（TDD）
- gtestを追加し、ロジックを分離したクラスをユニットテスト：
  - dtチェック：境界内/外で積分がスキップされること
  - バイアス推定：一定角速度をNサンプル後に引けていること
  - 角速度クリッピング：閾値超過が制限されること
  - ローパス：入力ステップに対する出力が平滑化されること
  - 姿勢積分：一定角速度でyaw/roll/pitchが線形に増えること（dt一定）
- ノードレベルは最小限（起動とパラメータ宣言確認）で、ロジックはユニットに閉じる。

## タスク
1. パッケージ依存追加（rclcpp, sensor_msgs, visualization_msgs, tf2）とテスト依存（ament_cmake_gtest, Eigenなど標準）。
2. ロジッククラス実装（フィルタ・積分・パラメータ保持）。
3. ノード実装：パラメータ宣言、IMU購読、Marker publish。
4. gtest作成＆実行（可能なら手元で実行、不可なら手順記載）。
